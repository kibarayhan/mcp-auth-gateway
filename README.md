# mcp-auth-gateway
A transparent reverse proxy that adds enterprise security to any MCP server — authentication, authorization, rate limiting, PII filtering, and audit logging. Zero changes to existing servers.
